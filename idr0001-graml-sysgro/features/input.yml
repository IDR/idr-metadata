---

server:
    host: localhost
    user: username
    # If password is empty you will be prompted
    password:
    # Optional: Connect via a SOCKS proxy
    #socksproxy:
    #    host: localhost
    #    port: 1080

screenid: 1

features: sysgroFeature_repeat1.tsv
progresslist: sysgro_progress.txt
errorlist: sysgro_errors.txt

metadatacolumns:
    - reference: str
    - name: str
    - description: str
    - alternativeName: str
    - experimentName: str
    - imageName: str
    - plateName: str
    - well: str
    - strainID: str

roicolumn: coordinates
